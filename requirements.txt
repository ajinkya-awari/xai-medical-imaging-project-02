# ─── XAI Chest X-ray Classification ───
#
# Install PyTorch FIRST (with or without CUDA):
#   CPU only:  pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#   CUDA 11.8: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#   CUDA 12.1: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# Then install the rest:
#   pip install -r requirements.txt
#
# Hugging Face Spaces builds this file directly, so keep the CPU wheel index
# here as well as in the Dockerfile's dedicated PyTorch installation layer.
torch==2.11.0
torchvision==0.26.0

numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
opencv-python-headless>=4.8.0
Pillow>=10.0.0
gradio>=4.0.0
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
huggingface_hub>=0.23.0
shap>=0.44
captum>=0.7
tqdm>=4.65.0
gdown
wandb
